# Assets

Owner: `one-person-lab-cloud`
Purpose: `public_asset_inventory`
State: `active_support`
Machine boundary: Human-readable asset inventory. The image files are the
actual assets; this page does not define product, runtime, or release truth.

This directory is reserved for OPL Cloud product maps, architecture diagrams,
and public screenshots.

`branding/opl-cloud-logo.png` is the public OPL Cloud logo used at the top of
both README variants. `branding/opl-cloud-overview-v2.png` is the public README
journey from local work through online continuation, private data, remote
compute, shared review, and optional publication. Deeper architecture notes may
still use Mermaid diagrams directly in Markdown so GitHub can render them
without generated image files.
