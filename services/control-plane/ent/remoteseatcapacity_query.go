// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"opl-cloud/services/control-plane/ent/predicate"
	"opl-cloud/services/control-plane/ent/remoteseatcapacity"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// RemoteSeatCapacityQuery is the builder for querying RemoteSeatCapacity entities.
type RemoteSeatCapacityQuery struct {
	config
	ctx        *QueryContext
	order      []remoteseatcapacity.OrderOption
	inters     []Interceptor
	predicates []predicate.RemoteSeatCapacity
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RemoteSeatCapacityQuery builder.
func (rscq *RemoteSeatCapacityQuery) Where(ps ...predicate.RemoteSeatCapacity) *RemoteSeatCapacityQuery {
	rscq.predicates = append(rscq.predicates, ps...)
	return rscq
}

// Limit the number of records to be returned by this query.
func (rscq *RemoteSeatCapacityQuery) Limit(limit int) *RemoteSeatCapacityQuery {
	rscq.ctx.Limit = &limit
	return rscq
}

// Offset to start from.
func (rscq *RemoteSeatCapacityQuery) Offset(offset int) *RemoteSeatCapacityQuery {
	rscq.ctx.Offset = &offset
	return rscq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rscq *RemoteSeatCapacityQuery) Unique(unique bool) *RemoteSeatCapacityQuery {
	rscq.ctx.Unique = &unique
	return rscq
}

// Order specifies how the records should be ordered.
func (rscq *RemoteSeatCapacityQuery) Order(o ...remoteseatcapacity.OrderOption) *RemoteSeatCapacityQuery {
	rscq.order = append(rscq.order, o...)
	return rscq
}

// First returns the first RemoteSeatCapacity entity from the query.
// Returns a *NotFoundError when no RemoteSeatCapacity was found.
func (rscq *RemoteSeatCapacityQuery) First(ctx context.Context) (*RemoteSeatCapacity, error) {
	nodes, err := rscq.Limit(1).All(setContextOp(ctx, rscq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{remoteseatcapacity.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rscq *RemoteSeatCapacityQuery) FirstX(ctx context.Context) *RemoteSeatCapacity {
	node, err := rscq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RemoteSeatCapacity ID from the query.
// Returns a *NotFoundError when no RemoteSeatCapacity ID was found.
func (rscq *RemoteSeatCapacityQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = rscq.Limit(1).IDs(setContextOp(ctx, rscq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{remoteseatcapacity.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rscq *RemoteSeatCapacityQuery) FirstIDX(ctx context.Context) string {
	id, err := rscq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RemoteSeatCapacity entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RemoteSeatCapacity entity is found.
// Returns a *NotFoundError when no RemoteSeatCapacity entities are found.
func (rscq *RemoteSeatCapacityQuery) Only(ctx context.Context) (*RemoteSeatCapacity, error) {
	nodes, err := rscq.Limit(2).All(setContextOp(ctx, rscq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{remoteseatcapacity.Label}
	default:
		return nil, &NotSingularError{remoteseatcapacity.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rscq *RemoteSeatCapacityQuery) OnlyX(ctx context.Context) *RemoteSeatCapacity {
	node, err := rscq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RemoteSeatCapacity ID in the query.
// Returns a *NotSingularError when more than one RemoteSeatCapacity ID is found.
// Returns a *NotFoundError when no entities are found.
func (rscq *RemoteSeatCapacityQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = rscq.Limit(2).IDs(setContextOp(ctx, rscq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{remoteseatcapacity.Label}
	default:
		err = &NotSingularError{remoteseatcapacity.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rscq *RemoteSeatCapacityQuery) OnlyIDX(ctx context.Context) string {
	id, err := rscq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RemoteSeatCapacities.
func (rscq *RemoteSeatCapacityQuery) All(ctx context.Context) ([]*RemoteSeatCapacity, error) {
	ctx = setContextOp(ctx, rscq.ctx, "All")
	if err := rscq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RemoteSeatCapacity, *RemoteSeatCapacityQuery]()
	return withInterceptors[[]*RemoteSeatCapacity](ctx, rscq, qr, rscq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rscq *RemoteSeatCapacityQuery) AllX(ctx context.Context) []*RemoteSeatCapacity {
	nodes, err := rscq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RemoteSeatCapacity IDs.
func (rscq *RemoteSeatCapacityQuery) IDs(ctx context.Context) (ids []string, err error) {
	if rscq.ctx.Unique == nil && rscq.path != nil {
		rscq.Unique(true)
	}
	ctx = setContextOp(ctx, rscq.ctx, "IDs")
	if err = rscq.Select(remoteseatcapacity.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rscq *RemoteSeatCapacityQuery) IDsX(ctx context.Context) []string {
	ids, err := rscq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rscq *RemoteSeatCapacityQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rscq.ctx, "Count")
	if err := rscq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rscq, querierCount[*RemoteSeatCapacityQuery](), rscq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rscq *RemoteSeatCapacityQuery) CountX(ctx context.Context) int {
	count, err := rscq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rscq *RemoteSeatCapacityQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rscq.ctx, "Exist")
	switch _, err := rscq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rscq *RemoteSeatCapacityQuery) ExistX(ctx context.Context) bool {
	exist, err := rscq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RemoteSeatCapacityQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rscq *RemoteSeatCapacityQuery) Clone() *RemoteSeatCapacityQuery {
	if rscq == nil {
		return nil
	}
	return &RemoteSeatCapacityQuery{
		config:     rscq.config,
		ctx:        rscq.ctx.Clone(),
		order:      append([]remoteseatcapacity.OrderOption{}, rscq.order...),
		inters:     append([]Interceptor{}, rscq.inters...),
		predicates: append([]predicate.RemoteSeatCapacity{}, rscq.predicates...),
		// clone intermediate query.
		sql:  rscq.sql.Clone(),
		path: rscq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RemoteSeatCapacity.Query().
//		GroupBy(remoteseatcapacity.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rscq *RemoteSeatCapacityQuery) GroupBy(field string, fields ...string) *RemoteSeatCapacityGroupBy {
	rscq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RemoteSeatCapacityGroupBy{build: rscq}
	grbuild.flds = &rscq.ctx.Fields
	grbuild.label = remoteseatcapacity.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.RemoteSeatCapacity.Query().
//		Select(remoteseatcapacity.FieldCreatedAt).
//		Scan(ctx, &v)
func (rscq *RemoteSeatCapacityQuery) Select(fields ...string) *RemoteSeatCapacitySelect {
	rscq.ctx.Fields = append(rscq.ctx.Fields, fields...)
	sbuild := &RemoteSeatCapacitySelect{RemoteSeatCapacityQuery: rscq}
	sbuild.label = remoteseatcapacity.Label
	sbuild.flds, sbuild.scan = &rscq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RemoteSeatCapacitySelect configured with the given aggregations.
func (rscq *RemoteSeatCapacityQuery) Aggregate(fns ...AggregateFunc) *RemoteSeatCapacitySelect {
	return rscq.Select().Aggregate(fns...)
}

func (rscq *RemoteSeatCapacityQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rscq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rscq); err != nil {
				return err
			}
		}
	}
	for _, f := range rscq.ctx.Fields {
		if !remoteseatcapacity.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rscq.path != nil {
		prev, err := rscq.path(ctx)
		if err != nil {
			return err
		}
		rscq.sql = prev
	}
	return nil
}

func (rscq *RemoteSeatCapacityQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RemoteSeatCapacity, error) {
	var (
		nodes = []*RemoteSeatCapacity{}
		_spec = rscq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RemoteSeatCapacity).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RemoteSeatCapacity{config: rscq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rscq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (rscq *RemoteSeatCapacityQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rscq.querySpec()
	_spec.Node.Columns = rscq.ctx.Fields
	if len(rscq.ctx.Fields) > 0 {
		_spec.Unique = rscq.ctx.Unique != nil && *rscq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rscq.driver, _spec)
}

func (rscq *RemoteSeatCapacityQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(remoteseatcapacity.Table, remoteseatcapacity.Columns, sqlgraph.NewFieldSpec(remoteseatcapacity.FieldID, field.TypeString))
	_spec.From = rscq.sql
	if unique := rscq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rscq.path != nil {
		_spec.Unique = true
	}
	if fields := rscq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, remoteseatcapacity.FieldID)
		for i := range fields {
			if fields[i] != remoteseatcapacity.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rscq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rscq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rscq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rscq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rscq *RemoteSeatCapacityQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rscq.driver.Dialect())
	t1 := builder.Table(remoteseatcapacity.Table)
	columns := rscq.ctx.Fields
	if len(columns) == 0 {
		columns = remoteseatcapacity.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rscq.sql != nil {
		selector = rscq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rscq.ctx.Unique != nil && *rscq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rscq.predicates {
		p(selector)
	}
	for _, p := range rscq.order {
		p(selector)
	}
	if offset := rscq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rscq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RemoteSeatCapacityGroupBy is the group-by builder for RemoteSeatCapacity entities.
type RemoteSeatCapacityGroupBy struct {
	selector
	build *RemoteSeatCapacityQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rscgb *RemoteSeatCapacityGroupBy) Aggregate(fns ...AggregateFunc) *RemoteSeatCapacityGroupBy {
	rscgb.fns = append(rscgb.fns, fns...)
	return rscgb
}

// Scan applies the selector query and scans the result into the given value.
func (rscgb *RemoteSeatCapacityGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rscgb.build.ctx, "GroupBy")
	if err := rscgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RemoteSeatCapacityQuery, *RemoteSeatCapacityGroupBy](ctx, rscgb.build, rscgb, rscgb.build.inters, v)
}

func (rscgb *RemoteSeatCapacityGroupBy) sqlScan(ctx context.Context, root *RemoteSeatCapacityQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rscgb.fns))
	for _, fn := range rscgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rscgb.flds)+len(rscgb.fns))
		for _, f := range *rscgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rscgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rscgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RemoteSeatCapacitySelect is the builder for selecting fields of RemoteSeatCapacity entities.
type RemoteSeatCapacitySelect struct {
	*RemoteSeatCapacityQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rscs *RemoteSeatCapacitySelect) Aggregate(fns ...AggregateFunc) *RemoteSeatCapacitySelect {
	rscs.fns = append(rscs.fns, fns...)
	return rscs
}

// Scan applies the selector query and scans the result into the given value.
func (rscs *RemoteSeatCapacitySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rscs.ctx, "Select")
	if err := rscs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RemoteSeatCapacityQuery, *RemoteSeatCapacitySelect](ctx, rscs.RemoteSeatCapacityQuery, rscs, rscs.inters, v)
}

func (rscs *RemoteSeatCapacitySelect) sqlScan(ctx context.Context, root *RemoteSeatCapacityQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rscs.fns))
	for _, fn := range rscs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rscs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rscs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
