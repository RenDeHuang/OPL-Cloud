// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"opl-cloud/services/control-plane/ent/remoteseatcapacity"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// RemoteSeatCapacity is the model entity for the RemoteSeatCapacity schema.
type RemoteSeatCapacity struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// SeatCount holds the value of the "seat_count" field.
	SeatCount int `json:"seat_count,omitempty"`
	// SeatLimit holds the value of the "seat_limit" field.
	SeatLimit int `json:"seat_limit,omitempty"`
	// WarningThreshold holds the value of the "warning_threshold" field.
	WarningThreshold int `json:"warning_threshold,omitempty"`
	selectValues     sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RemoteSeatCapacity) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case remoteseatcapacity.FieldSeatCount, remoteseatcapacity.FieldSeatLimit, remoteseatcapacity.FieldWarningThreshold:
			values[i] = new(sql.NullInt64)
		case remoteseatcapacity.FieldID:
			values[i] = new(sql.NullString)
		case remoteseatcapacity.FieldCreatedAt, remoteseatcapacity.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RemoteSeatCapacity fields.
func (rsc *RemoteSeatCapacity) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case remoteseatcapacity.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				rsc.ID = value.String
			}
		case remoteseatcapacity.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				rsc.CreatedAt = value.Time
			}
		case remoteseatcapacity.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				rsc.UpdatedAt = value.Time
			}
		case remoteseatcapacity.FieldSeatCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field seat_count", values[i])
			} else if value.Valid {
				rsc.SeatCount = int(value.Int64)
			}
		case remoteseatcapacity.FieldSeatLimit:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field seat_limit", values[i])
			} else if value.Valid {
				rsc.SeatLimit = int(value.Int64)
			}
		case remoteseatcapacity.FieldWarningThreshold:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field warning_threshold", values[i])
			} else if value.Valid {
				rsc.WarningThreshold = int(value.Int64)
			}
		default:
			rsc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RemoteSeatCapacity.
// This includes values selected through modifiers, order, etc.
func (rsc *RemoteSeatCapacity) Value(name string) (ent.Value, error) {
	return rsc.selectValues.Get(name)
}

// Update returns a builder for updating this RemoteSeatCapacity.
// Note that you need to call RemoteSeatCapacity.Unwrap() before calling this method if this RemoteSeatCapacity
// was returned from a transaction, and the transaction was committed or rolled back.
func (rsc *RemoteSeatCapacity) Update() *RemoteSeatCapacityUpdateOne {
	return NewRemoteSeatCapacityClient(rsc.config).UpdateOne(rsc)
}

// Unwrap unwraps the RemoteSeatCapacity entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (rsc *RemoteSeatCapacity) Unwrap() *RemoteSeatCapacity {
	_tx, ok := rsc.config.driver.(*txDriver)
	if !ok {
		panic("ent: RemoteSeatCapacity is not a transactional entity")
	}
	rsc.config.driver = _tx.drv
	return rsc
}

// String implements the fmt.Stringer.
func (rsc *RemoteSeatCapacity) String() string {
	var builder strings.Builder
	builder.WriteString("RemoteSeatCapacity(")
	builder.WriteString(fmt.Sprintf("id=%v, ", rsc.ID))
	builder.WriteString("created_at=")
	builder.WriteString(rsc.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(rsc.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("seat_count=")
	builder.WriteString(fmt.Sprintf("%v", rsc.SeatCount))
	builder.WriteString(", ")
	builder.WriteString("seat_limit=")
	builder.WriteString(fmt.Sprintf("%v", rsc.SeatLimit))
	builder.WriteString(", ")
	builder.WriteString("warning_threshold=")
	builder.WriteString(fmt.Sprintf("%v", rsc.WarningThreshold))
	builder.WriteByte(')')
	return builder.String()
}

// RemoteSeatCapacities is a parsable slice of RemoteSeatCapacity.
type RemoteSeatCapacities []*RemoteSeatCapacity
