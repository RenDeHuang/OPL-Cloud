// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"opl-cloud/services/control-plane/ent/predicate"
	"opl-cloud/services/control-plane/ent/remotedevicecredential"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// RemoteDeviceCredentialQuery is the builder for querying RemoteDeviceCredential entities.
type RemoteDeviceCredentialQuery struct {
	config
	ctx        *QueryContext
	order      []remotedevicecredential.OrderOption
	inters     []Interceptor
	predicates []predicate.RemoteDeviceCredential
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RemoteDeviceCredentialQuery builder.
func (rdcq *RemoteDeviceCredentialQuery) Where(ps ...predicate.RemoteDeviceCredential) *RemoteDeviceCredentialQuery {
	rdcq.predicates = append(rdcq.predicates, ps...)
	return rdcq
}

// Limit the number of records to be returned by this query.
func (rdcq *RemoteDeviceCredentialQuery) Limit(limit int) *RemoteDeviceCredentialQuery {
	rdcq.ctx.Limit = &limit
	return rdcq
}

// Offset to start from.
func (rdcq *RemoteDeviceCredentialQuery) Offset(offset int) *RemoteDeviceCredentialQuery {
	rdcq.ctx.Offset = &offset
	return rdcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rdcq *RemoteDeviceCredentialQuery) Unique(unique bool) *RemoteDeviceCredentialQuery {
	rdcq.ctx.Unique = &unique
	return rdcq
}

// Order specifies how the records should be ordered.
func (rdcq *RemoteDeviceCredentialQuery) Order(o ...remotedevicecredential.OrderOption) *RemoteDeviceCredentialQuery {
	rdcq.order = append(rdcq.order, o...)
	return rdcq
}

// First returns the first RemoteDeviceCredential entity from the query.
// Returns a *NotFoundError when no RemoteDeviceCredential was found.
func (rdcq *RemoteDeviceCredentialQuery) First(ctx context.Context) (*RemoteDeviceCredential, error) {
	nodes, err := rdcq.Limit(1).All(setContextOp(ctx, rdcq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{remotedevicecredential.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rdcq *RemoteDeviceCredentialQuery) FirstX(ctx context.Context) *RemoteDeviceCredential {
	node, err := rdcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RemoteDeviceCredential ID from the query.
// Returns a *NotFoundError when no RemoteDeviceCredential ID was found.
func (rdcq *RemoteDeviceCredentialQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = rdcq.Limit(1).IDs(setContextOp(ctx, rdcq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{remotedevicecredential.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rdcq *RemoteDeviceCredentialQuery) FirstIDX(ctx context.Context) string {
	id, err := rdcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RemoteDeviceCredential entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RemoteDeviceCredential entity is found.
// Returns a *NotFoundError when no RemoteDeviceCredential entities are found.
func (rdcq *RemoteDeviceCredentialQuery) Only(ctx context.Context) (*RemoteDeviceCredential, error) {
	nodes, err := rdcq.Limit(2).All(setContextOp(ctx, rdcq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{remotedevicecredential.Label}
	default:
		return nil, &NotSingularError{remotedevicecredential.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rdcq *RemoteDeviceCredentialQuery) OnlyX(ctx context.Context) *RemoteDeviceCredential {
	node, err := rdcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RemoteDeviceCredential ID in the query.
// Returns a *NotSingularError when more than one RemoteDeviceCredential ID is found.
// Returns a *NotFoundError when no entities are found.
func (rdcq *RemoteDeviceCredentialQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = rdcq.Limit(2).IDs(setContextOp(ctx, rdcq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{remotedevicecredential.Label}
	default:
		err = &NotSingularError{remotedevicecredential.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rdcq *RemoteDeviceCredentialQuery) OnlyIDX(ctx context.Context) string {
	id, err := rdcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RemoteDeviceCredentials.
func (rdcq *RemoteDeviceCredentialQuery) All(ctx context.Context) ([]*RemoteDeviceCredential, error) {
	ctx = setContextOp(ctx, rdcq.ctx, "All")
	if err := rdcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RemoteDeviceCredential, *RemoteDeviceCredentialQuery]()
	return withInterceptors[[]*RemoteDeviceCredential](ctx, rdcq, qr, rdcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rdcq *RemoteDeviceCredentialQuery) AllX(ctx context.Context) []*RemoteDeviceCredential {
	nodes, err := rdcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RemoteDeviceCredential IDs.
func (rdcq *RemoteDeviceCredentialQuery) IDs(ctx context.Context) (ids []string, err error) {
	if rdcq.ctx.Unique == nil && rdcq.path != nil {
		rdcq.Unique(true)
	}
	ctx = setContextOp(ctx, rdcq.ctx, "IDs")
	if err = rdcq.Select(remotedevicecredential.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rdcq *RemoteDeviceCredentialQuery) IDsX(ctx context.Context) []string {
	ids, err := rdcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rdcq *RemoteDeviceCredentialQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rdcq.ctx, "Count")
	if err := rdcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rdcq, querierCount[*RemoteDeviceCredentialQuery](), rdcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rdcq *RemoteDeviceCredentialQuery) CountX(ctx context.Context) int {
	count, err := rdcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rdcq *RemoteDeviceCredentialQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rdcq.ctx, "Exist")
	switch _, err := rdcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rdcq *RemoteDeviceCredentialQuery) ExistX(ctx context.Context) bool {
	exist, err := rdcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RemoteDeviceCredentialQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rdcq *RemoteDeviceCredentialQuery) Clone() *RemoteDeviceCredentialQuery {
	if rdcq == nil {
		return nil
	}
	return &RemoteDeviceCredentialQuery{
		config:     rdcq.config,
		ctx:        rdcq.ctx.Clone(),
		order:      append([]remotedevicecredential.OrderOption{}, rdcq.order...),
		inters:     append([]Interceptor{}, rdcq.inters...),
		predicates: append([]predicate.RemoteDeviceCredential{}, rdcq.predicates...),
		// clone intermediate query.
		sql:  rdcq.sql.Clone(),
		path: rdcq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RemoteDeviceCredential.Query().
//		GroupBy(remotedevicecredential.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rdcq *RemoteDeviceCredentialQuery) GroupBy(field string, fields ...string) *RemoteDeviceCredentialGroupBy {
	rdcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RemoteDeviceCredentialGroupBy{build: rdcq}
	grbuild.flds = &rdcq.ctx.Fields
	grbuild.label = remotedevicecredential.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.RemoteDeviceCredential.Query().
//		Select(remotedevicecredential.FieldCreatedAt).
//		Scan(ctx, &v)
func (rdcq *RemoteDeviceCredentialQuery) Select(fields ...string) *RemoteDeviceCredentialSelect {
	rdcq.ctx.Fields = append(rdcq.ctx.Fields, fields...)
	sbuild := &RemoteDeviceCredentialSelect{RemoteDeviceCredentialQuery: rdcq}
	sbuild.label = remotedevicecredential.Label
	sbuild.flds, sbuild.scan = &rdcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RemoteDeviceCredentialSelect configured with the given aggregations.
func (rdcq *RemoteDeviceCredentialQuery) Aggregate(fns ...AggregateFunc) *RemoteDeviceCredentialSelect {
	return rdcq.Select().Aggregate(fns...)
}

func (rdcq *RemoteDeviceCredentialQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rdcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rdcq); err != nil {
				return err
			}
		}
	}
	for _, f := range rdcq.ctx.Fields {
		if !remotedevicecredential.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rdcq.path != nil {
		prev, err := rdcq.path(ctx)
		if err != nil {
			return err
		}
		rdcq.sql = prev
	}
	return nil
}

func (rdcq *RemoteDeviceCredentialQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RemoteDeviceCredential, error) {
	var (
		nodes = []*RemoteDeviceCredential{}
		_spec = rdcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RemoteDeviceCredential).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RemoteDeviceCredential{config: rdcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rdcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (rdcq *RemoteDeviceCredentialQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rdcq.querySpec()
	_spec.Node.Columns = rdcq.ctx.Fields
	if len(rdcq.ctx.Fields) > 0 {
		_spec.Unique = rdcq.ctx.Unique != nil && *rdcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rdcq.driver, _spec)
}

func (rdcq *RemoteDeviceCredentialQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(remotedevicecredential.Table, remotedevicecredential.Columns, sqlgraph.NewFieldSpec(remotedevicecredential.FieldID, field.TypeString))
	_spec.From = rdcq.sql
	if unique := rdcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rdcq.path != nil {
		_spec.Unique = true
	}
	if fields := rdcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, remotedevicecredential.FieldID)
		for i := range fields {
			if fields[i] != remotedevicecredential.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rdcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rdcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rdcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rdcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rdcq *RemoteDeviceCredentialQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rdcq.driver.Dialect())
	t1 := builder.Table(remotedevicecredential.Table)
	columns := rdcq.ctx.Fields
	if len(columns) == 0 {
		columns = remotedevicecredential.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rdcq.sql != nil {
		selector = rdcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rdcq.ctx.Unique != nil && *rdcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rdcq.predicates {
		p(selector)
	}
	for _, p := range rdcq.order {
		p(selector)
	}
	if offset := rdcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rdcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RemoteDeviceCredentialGroupBy is the group-by builder for RemoteDeviceCredential entities.
type RemoteDeviceCredentialGroupBy struct {
	selector
	build *RemoteDeviceCredentialQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rdcgb *RemoteDeviceCredentialGroupBy) Aggregate(fns ...AggregateFunc) *RemoteDeviceCredentialGroupBy {
	rdcgb.fns = append(rdcgb.fns, fns...)
	return rdcgb
}

// Scan applies the selector query and scans the result into the given value.
func (rdcgb *RemoteDeviceCredentialGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rdcgb.build.ctx, "GroupBy")
	if err := rdcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RemoteDeviceCredentialQuery, *RemoteDeviceCredentialGroupBy](ctx, rdcgb.build, rdcgb, rdcgb.build.inters, v)
}

func (rdcgb *RemoteDeviceCredentialGroupBy) sqlScan(ctx context.Context, root *RemoteDeviceCredentialQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rdcgb.fns))
	for _, fn := range rdcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rdcgb.flds)+len(rdcgb.fns))
		for _, f := range *rdcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rdcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rdcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RemoteDeviceCredentialSelect is the builder for selecting fields of RemoteDeviceCredential entities.
type RemoteDeviceCredentialSelect struct {
	*RemoteDeviceCredentialQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rdcs *RemoteDeviceCredentialSelect) Aggregate(fns ...AggregateFunc) *RemoteDeviceCredentialSelect {
	rdcs.fns = append(rdcs.fns, fns...)
	return rdcs
}

// Scan applies the selector query and scans the result into the given value.
func (rdcs *RemoteDeviceCredentialSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rdcs.ctx, "Select")
	if err := rdcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RemoteDeviceCredentialQuery, *RemoteDeviceCredentialSelect](ctx, rdcs.RemoteDeviceCredentialQuery, rdcs, rdcs.inters, v)
}

func (rdcs *RemoteDeviceCredentialSelect) sqlScan(ctx context.Context, root *RemoteDeviceCredentialQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rdcs.fns))
	for _, fn := range rdcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rdcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rdcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
