// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"opl-cloud/services/control-plane/ent/predicate"
	"opl-cloud/services/control-plane/ent/remotepairing"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// RemotePairingQuery is the builder for querying RemotePairing entities.
type RemotePairingQuery struct {
	config
	ctx        *QueryContext
	order      []remotepairing.OrderOption
	inters     []Interceptor
	predicates []predicate.RemotePairing
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RemotePairingQuery builder.
func (rpq *RemotePairingQuery) Where(ps ...predicate.RemotePairing) *RemotePairingQuery {
	rpq.predicates = append(rpq.predicates, ps...)
	return rpq
}

// Limit the number of records to be returned by this query.
func (rpq *RemotePairingQuery) Limit(limit int) *RemotePairingQuery {
	rpq.ctx.Limit = &limit
	return rpq
}

// Offset to start from.
func (rpq *RemotePairingQuery) Offset(offset int) *RemotePairingQuery {
	rpq.ctx.Offset = &offset
	return rpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rpq *RemotePairingQuery) Unique(unique bool) *RemotePairingQuery {
	rpq.ctx.Unique = &unique
	return rpq
}

// Order specifies how the records should be ordered.
func (rpq *RemotePairingQuery) Order(o ...remotepairing.OrderOption) *RemotePairingQuery {
	rpq.order = append(rpq.order, o...)
	return rpq
}

// First returns the first RemotePairing entity from the query.
// Returns a *NotFoundError when no RemotePairing was found.
func (rpq *RemotePairingQuery) First(ctx context.Context) (*RemotePairing, error) {
	nodes, err := rpq.Limit(1).All(setContextOp(ctx, rpq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{remotepairing.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rpq *RemotePairingQuery) FirstX(ctx context.Context) *RemotePairing {
	node, err := rpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RemotePairing ID from the query.
// Returns a *NotFoundError when no RemotePairing ID was found.
func (rpq *RemotePairingQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = rpq.Limit(1).IDs(setContextOp(ctx, rpq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{remotepairing.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rpq *RemotePairingQuery) FirstIDX(ctx context.Context) string {
	id, err := rpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RemotePairing entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RemotePairing entity is found.
// Returns a *NotFoundError when no RemotePairing entities are found.
func (rpq *RemotePairingQuery) Only(ctx context.Context) (*RemotePairing, error) {
	nodes, err := rpq.Limit(2).All(setContextOp(ctx, rpq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{remotepairing.Label}
	default:
		return nil, &NotSingularError{remotepairing.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rpq *RemotePairingQuery) OnlyX(ctx context.Context) *RemotePairing {
	node, err := rpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RemotePairing ID in the query.
// Returns a *NotSingularError when more than one RemotePairing ID is found.
// Returns a *NotFoundError when no entities are found.
func (rpq *RemotePairingQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = rpq.Limit(2).IDs(setContextOp(ctx, rpq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{remotepairing.Label}
	default:
		err = &NotSingularError{remotepairing.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rpq *RemotePairingQuery) OnlyIDX(ctx context.Context) string {
	id, err := rpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RemotePairings.
func (rpq *RemotePairingQuery) All(ctx context.Context) ([]*RemotePairing, error) {
	ctx = setContextOp(ctx, rpq.ctx, "All")
	if err := rpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RemotePairing, *RemotePairingQuery]()
	return withInterceptors[[]*RemotePairing](ctx, rpq, qr, rpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rpq *RemotePairingQuery) AllX(ctx context.Context) []*RemotePairing {
	nodes, err := rpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RemotePairing IDs.
func (rpq *RemotePairingQuery) IDs(ctx context.Context) (ids []string, err error) {
	if rpq.ctx.Unique == nil && rpq.path != nil {
		rpq.Unique(true)
	}
	ctx = setContextOp(ctx, rpq.ctx, "IDs")
	if err = rpq.Select(remotepairing.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rpq *RemotePairingQuery) IDsX(ctx context.Context) []string {
	ids, err := rpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rpq *RemotePairingQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rpq.ctx, "Count")
	if err := rpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rpq, querierCount[*RemotePairingQuery](), rpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rpq *RemotePairingQuery) CountX(ctx context.Context) int {
	count, err := rpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rpq *RemotePairingQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rpq.ctx, "Exist")
	switch _, err := rpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rpq *RemotePairingQuery) ExistX(ctx context.Context) bool {
	exist, err := rpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RemotePairingQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rpq *RemotePairingQuery) Clone() *RemotePairingQuery {
	if rpq == nil {
		return nil
	}
	return &RemotePairingQuery{
		config:     rpq.config,
		ctx:        rpq.ctx.Clone(),
		order:      append([]remotepairing.OrderOption{}, rpq.order...),
		inters:     append([]Interceptor{}, rpq.inters...),
		predicates: append([]predicate.RemotePairing{}, rpq.predicates...),
		// clone intermediate query.
		sql:  rpq.sql.Clone(),
		path: rpq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RemotePairing.Query().
//		GroupBy(remotepairing.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rpq *RemotePairingQuery) GroupBy(field string, fields ...string) *RemotePairingGroupBy {
	rpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RemotePairingGroupBy{build: rpq}
	grbuild.flds = &rpq.ctx.Fields
	grbuild.label = remotepairing.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.RemotePairing.Query().
//		Select(remotepairing.FieldCreatedAt).
//		Scan(ctx, &v)
func (rpq *RemotePairingQuery) Select(fields ...string) *RemotePairingSelect {
	rpq.ctx.Fields = append(rpq.ctx.Fields, fields...)
	sbuild := &RemotePairingSelect{RemotePairingQuery: rpq}
	sbuild.label = remotepairing.Label
	sbuild.flds, sbuild.scan = &rpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RemotePairingSelect configured with the given aggregations.
func (rpq *RemotePairingQuery) Aggregate(fns ...AggregateFunc) *RemotePairingSelect {
	return rpq.Select().Aggregate(fns...)
}

func (rpq *RemotePairingQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rpq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rpq); err != nil {
				return err
			}
		}
	}
	for _, f := range rpq.ctx.Fields {
		if !remotepairing.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rpq.path != nil {
		prev, err := rpq.path(ctx)
		if err != nil {
			return err
		}
		rpq.sql = prev
	}
	return nil
}

func (rpq *RemotePairingQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RemotePairing, error) {
	var (
		nodes = []*RemotePairing{}
		_spec = rpq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RemotePairing).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RemotePairing{config: rpq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (rpq *RemotePairingQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rpq.querySpec()
	_spec.Node.Columns = rpq.ctx.Fields
	if len(rpq.ctx.Fields) > 0 {
		_spec.Unique = rpq.ctx.Unique != nil && *rpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rpq.driver, _spec)
}

func (rpq *RemotePairingQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(remotepairing.Table, remotepairing.Columns, sqlgraph.NewFieldSpec(remotepairing.FieldID, field.TypeString))
	_spec.From = rpq.sql
	if unique := rpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rpq.path != nil {
		_spec.Unique = true
	}
	if fields := rpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, remotepairing.FieldID)
		for i := range fields {
			if fields[i] != remotepairing.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rpq *RemotePairingQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rpq.driver.Dialect())
	t1 := builder.Table(remotepairing.Table)
	columns := rpq.ctx.Fields
	if len(columns) == 0 {
		columns = remotepairing.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rpq.sql != nil {
		selector = rpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rpq.ctx.Unique != nil && *rpq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rpq.predicates {
		p(selector)
	}
	for _, p := range rpq.order {
		p(selector)
	}
	if offset := rpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RemotePairingGroupBy is the group-by builder for RemotePairing entities.
type RemotePairingGroupBy struct {
	selector
	build *RemotePairingQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rpgb *RemotePairingGroupBy) Aggregate(fns ...AggregateFunc) *RemotePairingGroupBy {
	rpgb.fns = append(rpgb.fns, fns...)
	return rpgb
}

// Scan applies the selector query and scans the result into the given value.
func (rpgb *RemotePairingGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rpgb.build.ctx, "GroupBy")
	if err := rpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RemotePairingQuery, *RemotePairingGroupBy](ctx, rpgb.build, rpgb, rpgb.build.inters, v)
}

func (rpgb *RemotePairingGroupBy) sqlScan(ctx context.Context, root *RemotePairingQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rpgb.fns))
	for _, fn := range rpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rpgb.flds)+len(rpgb.fns))
		for _, f := range *rpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RemotePairingSelect is the builder for selecting fields of RemotePairing entities.
type RemotePairingSelect struct {
	*RemotePairingQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rps *RemotePairingSelect) Aggregate(fns ...AggregateFunc) *RemotePairingSelect {
	rps.fns = append(rps.fns, fns...)
	return rps
}

// Scan applies the selector query and scans the result into the given value.
func (rps *RemotePairingSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rps.ctx, "Select")
	if err := rps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RemotePairingQuery, *RemotePairingSelect](ctx, rps.RemotePairingQuery, rps, rps.inters, v)
}

func (rps *RemotePairingSelect) sqlScan(ctx context.Context, root *RemotePairingQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rps.fns))
	for _, fn := range rps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
