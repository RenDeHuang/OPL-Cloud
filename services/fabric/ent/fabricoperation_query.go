// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"opl-cloud/services/fabric/ent/fabricoperation"
	"opl-cloud/services/fabric/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// FabricOperationQuery is the builder for querying FabricOperation entities.
type FabricOperationQuery struct {
	config
	ctx        *QueryContext
	order      []fabricoperation.OrderOption
	inters     []Interceptor
	predicates []predicate.FabricOperation
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FabricOperationQuery builder.
func (foq *FabricOperationQuery) Where(ps ...predicate.FabricOperation) *FabricOperationQuery {
	foq.predicates = append(foq.predicates, ps...)
	return foq
}

// Limit the number of records to be returned by this query.
func (foq *FabricOperationQuery) Limit(limit int) *FabricOperationQuery {
	foq.ctx.Limit = &limit
	return foq
}

// Offset to start from.
func (foq *FabricOperationQuery) Offset(offset int) *FabricOperationQuery {
	foq.ctx.Offset = &offset
	return foq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (foq *FabricOperationQuery) Unique(unique bool) *FabricOperationQuery {
	foq.ctx.Unique = &unique
	return foq
}

// Order specifies how the records should be ordered.
func (foq *FabricOperationQuery) Order(o ...fabricoperation.OrderOption) *FabricOperationQuery {
	foq.order = append(foq.order, o...)
	return foq
}

// First returns the first FabricOperation entity from the query.
// Returns a *NotFoundError when no FabricOperation was found.
func (foq *FabricOperationQuery) First(ctx context.Context) (*FabricOperation, error) {
	nodes, err := foq.Limit(1).All(setContextOp(ctx, foq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{fabricoperation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (foq *FabricOperationQuery) FirstX(ctx context.Context) *FabricOperation {
	node, err := foq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FabricOperation ID from the query.
// Returns a *NotFoundError when no FabricOperation ID was found.
func (foq *FabricOperationQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = foq.Limit(1).IDs(setContextOp(ctx, foq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{fabricoperation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (foq *FabricOperationQuery) FirstIDX(ctx context.Context) string {
	id, err := foq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FabricOperation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FabricOperation entity is found.
// Returns a *NotFoundError when no FabricOperation entities are found.
func (foq *FabricOperationQuery) Only(ctx context.Context) (*FabricOperation, error) {
	nodes, err := foq.Limit(2).All(setContextOp(ctx, foq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{fabricoperation.Label}
	default:
		return nil, &NotSingularError{fabricoperation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (foq *FabricOperationQuery) OnlyX(ctx context.Context) *FabricOperation {
	node, err := foq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FabricOperation ID in the query.
// Returns a *NotSingularError when more than one FabricOperation ID is found.
// Returns a *NotFoundError when no entities are found.
func (foq *FabricOperationQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = foq.Limit(2).IDs(setContextOp(ctx, foq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{fabricoperation.Label}
	default:
		err = &NotSingularError{fabricoperation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (foq *FabricOperationQuery) OnlyIDX(ctx context.Context) string {
	id, err := foq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FabricOperations.
func (foq *FabricOperationQuery) All(ctx context.Context) ([]*FabricOperation, error) {
	ctx = setContextOp(ctx, foq.ctx, "All")
	if err := foq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FabricOperation, *FabricOperationQuery]()
	return withInterceptors[[]*FabricOperation](ctx, foq, qr, foq.inters)
}

// AllX is like All, but panics if an error occurs.
func (foq *FabricOperationQuery) AllX(ctx context.Context) []*FabricOperation {
	nodes, err := foq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FabricOperation IDs.
func (foq *FabricOperationQuery) IDs(ctx context.Context) (ids []string, err error) {
	if foq.ctx.Unique == nil && foq.path != nil {
		foq.Unique(true)
	}
	ctx = setContextOp(ctx, foq.ctx, "IDs")
	if err = foq.Select(fabricoperation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (foq *FabricOperationQuery) IDsX(ctx context.Context) []string {
	ids, err := foq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (foq *FabricOperationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, foq.ctx, "Count")
	if err := foq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, foq, querierCount[*FabricOperationQuery](), foq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (foq *FabricOperationQuery) CountX(ctx context.Context) int {
	count, err := foq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (foq *FabricOperationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, foq.ctx, "Exist")
	switch _, err := foq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (foq *FabricOperationQuery) ExistX(ctx context.Context) bool {
	exist, err := foq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FabricOperationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (foq *FabricOperationQuery) Clone() *FabricOperationQuery {
	if foq == nil {
		return nil
	}
	return &FabricOperationQuery{
		config:     foq.config,
		ctx:        foq.ctx.Clone(),
		order:      append([]fabricoperation.OrderOption{}, foq.order...),
		inters:     append([]Interceptor{}, foq.inters...),
		predicates: append([]predicate.FabricOperation{}, foq.predicates...),
		// clone intermediate query.
		sql:  foq.sql.Clone(),
		path: foq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		OperationID string `json:"operation_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FabricOperation.Query().
//		GroupBy(fabricoperation.FieldOperationID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (foq *FabricOperationQuery) GroupBy(field string, fields ...string) *FabricOperationGroupBy {
	foq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FabricOperationGroupBy{build: foq}
	grbuild.flds = &foq.ctx.Fields
	grbuild.label = fabricoperation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		OperationID string `json:"operation_id,omitempty"`
//	}
//
//	client.FabricOperation.Query().
//		Select(fabricoperation.FieldOperationID).
//		Scan(ctx, &v)
func (foq *FabricOperationQuery) Select(fields ...string) *FabricOperationSelect {
	foq.ctx.Fields = append(foq.ctx.Fields, fields...)
	sbuild := &FabricOperationSelect{FabricOperationQuery: foq}
	sbuild.label = fabricoperation.Label
	sbuild.flds, sbuild.scan = &foq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FabricOperationSelect configured with the given aggregations.
func (foq *FabricOperationQuery) Aggregate(fns ...AggregateFunc) *FabricOperationSelect {
	return foq.Select().Aggregate(fns...)
}

func (foq *FabricOperationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range foq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, foq); err != nil {
				return err
			}
		}
	}
	for _, f := range foq.ctx.Fields {
		if !fabricoperation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if foq.path != nil {
		prev, err := foq.path(ctx)
		if err != nil {
			return err
		}
		foq.sql = prev
	}
	return nil
}

func (foq *FabricOperationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FabricOperation, error) {
	var (
		nodes = []*FabricOperation{}
		_spec = foq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FabricOperation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FabricOperation{config: foq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, foq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (foq *FabricOperationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := foq.querySpec()
	_spec.Node.Columns = foq.ctx.Fields
	if len(foq.ctx.Fields) > 0 {
		_spec.Unique = foq.ctx.Unique != nil && *foq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, foq.driver, _spec)
}

func (foq *FabricOperationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(fabricoperation.Table, fabricoperation.Columns, sqlgraph.NewFieldSpec(fabricoperation.FieldID, field.TypeString))
	_spec.From = foq.sql
	if unique := foq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if foq.path != nil {
		_spec.Unique = true
	}
	if fields := foq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, fabricoperation.FieldID)
		for i := range fields {
			if fields[i] != fabricoperation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := foq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := foq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := foq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := foq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (foq *FabricOperationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(foq.driver.Dialect())
	t1 := builder.Table(fabricoperation.Table)
	columns := foq.ctx.Fields
	if len(columns) == 0 {
		columns = fabricoperation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if foq.sql != nil {
		selector = foq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if foq.ctx.Unique != nil && *foq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range foq.predicates {
		p(selector)
	}
	for _, p := range foq.order {
		p(selector)
	}
	if offset := foq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := foq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FabricOperationGroupBy is the group-by builder for FabricOperation entities.
type FabricOperationGroupBy struct {
	selector
	build *FabricOperationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fogb *FabricOperationGroupBy) Aggregate(fns ...AggregateFunc) *FabricOperationGroupBy {
	fogb.fns = append(fogb.fns, fns...)
	return fogb
}

// Scan applies the selector query and scans the result into the given value.
func (fogb *FabricOperationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fogb.build.ctx, "GroupBy")
	if err := fogb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FabricOperationQuery, *FabricOperationGroupBy](ctx, fogb.build, fogb, fogb.build.inters, v)
}

func (fogb *FabricOperationGroupBy) sqlScan(ctx context.Context, root *FabricOperationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(fogb.fns))
	for _, fn := range fogb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*fogb.flds)+len(fogb.fns))
		for _, f := range *fogb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*fogb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fogb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FabricOperationSelect is the builder for selecting fields of FabricOperation entities.
type FabricOperationSelect struct {
	*FabricOperationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fos *FabricOperationSelect) Aggregate(fns ...AggregateFunc) *FabricOperationSelect {
	fos.fns = append(fos.fns, fns...)
	return fos
}

// Scan applies the selector query and scans the result into the given value.
func (fos *FabricOperationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fos.ctx, "Select")
	if err := fos.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FabricOperationQuery, *FabricOperationSelect](ctx, fos.FabricOperationQuery, fos, fos.inters, v)
}

func (fos *FabricOperationSelect) sqlScan(ctx context.Context, root *FabricOperationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fos.fns))
	for _, fn := range fos.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fos.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fos.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
