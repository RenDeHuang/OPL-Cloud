// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"opl-cloud/services/fabric/ent/predicate"
	"opl-cloud/services/fabric/ent/workspaceruntimeaccess"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// WorkspaceRuntimeAccessQuery is the builder for querying WorkspaceRuntimeAccess entities.
type WorkspaceRuntimeAccessQuery struct {
	config
	ctx        *QueryContext
	order      []workspaceruntimeaccess.OrderOption
	inters     []Interceptor
	predicates []predicate.WorkspaceRuntimeAccess
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WorkspaceRuntimeAccessQuery builder.
func (wraq *WorkspaceRuntimeAccessQuery) Where(ps ...predicate.WorkspaceRuntimeAccess) *WorkspaceRuntimeAccessQuery {
	wraq.predicates = append(wraq.predicates, ps...)
	return wraq
}

// Limit the number of records to be returned by this query.
func (wraq *WorkspaceRuntimeAccessQuery) Limit(limit int) *WorkspaceRuntimeAccessQuery {
	wraq.ctx.Limit = &limit
	return wraq
}

// Offset to start from.
func (wraq *WorkspaceRuntimeAccessQuery) Offset(offset int) *WorkspaceRuntimeAccessQuery {
	wraq.ctx.Offset = &offset
	return wraq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (wraq *WorkspaceRuntimeAccessQuery) Unique(unique bool) *WorkspaceRuntimeAccessQuery {
	wraq.ctx.Unique = &unique
	return wraq
}

// Order specifies how the records should be ordered.
func (wraq *WorkspaceRuntimeAccessQuery) Order(o ...workspaceruntimeaccess.OrderOption) *WorkspaceRuntimeAccessQuery {
	wraq.order = append(wraq.order, o...)
	return wraq
}

// First returns the first WorkspaceRuntimeAccess entity from the query.
// Returns a *NotFoundError when no WorkspaceRuntimeAccess was found.
func (wraq *WorkspaceRuntimeAccessQuery) First(ctx context.Context) (*WorkspaceRuntimeAccess, error) {
	nodes, err := wraq.Limit(1).All(setContextOp(ctx, wraq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{workspaceruntimeaccess.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (wraq *WorkspaceRuntimeAccessQuery) FirstX(ctx context.Context) *WorkspaceRuntimeAccess {
	node, err := wraq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WorkspaceRuntimeAccess ID from the query.
// Returns a *NotFoundError when no WorkspaceRuntimeAccess ID was found.
func (wraq *WorkspaceRuntimeAccessQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = wraq.Limit(1).IDs(setContextOp(ctx, wraq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{workspaceruntimeaccess.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (wraq *WorkspaceRuntimeAccessQuery) FirstIDX(ctx context.Context) string {
	id, err := wraq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WorkspaceRuntimeAccess entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WorkspaceRuntimeAccess entity is found.
// Returns a *NotFoundError when no WorkspaceRuntimeAccess entities are found.
func (wraq *WorkspaceRuntimeAccessQuery) Only(ctx context.Context) (*WorkspaceRuntimeAccess, error) {
	nodes, err := wraq.Limit(2).All(setContextOp(ctx, wraq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{workspaceruntimeaccess.Label}
	default:
		return nil, &NotSingularError{workspaceruntimeaccess.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (wraq *WorkspaceRuntimeAccessQuery) OnlyX(ctx context.Context) *WorkspaceRuntimeAccess {
	node, err := wraq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WorkspaceRuntimeAccess ID in the query.
// Returns a *NotSingularError when more than one WorkspaceRuntimeAccess ID is found.
// Returns a *NotFoundError when no entities are found.
func (wraq *WorkspaceRuntimeAccessQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = wraq.Limit(2).IDs(setContextOp(ctx, wraq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{workspaceruntimeaccess.Label}
	default:
		err = &NotSingularError{workspaceruntimeaccess.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (wraq *WorkspaceRuntimeAccessQuery) OnlyIDX(ctx context.Context) string {
	id, err := wraq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WorkspaceRuntimeAccesses.
func (wraq *WorkspaceRuntimeAccessQuery) All(ctx context.Context) ([]*WorkspaceRuntimeAccess, error) {
	ctx = setContextOp(ctx, wraq.ctx, "All")
	if err := wraq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WorkspaceRuntimeAccess, *WorkspaceRuntimeAccessQuery]()
	return withInterceptors[[]*WorkspaceRuntimeAccess](ctx, wraq, qr, wraq.inters)
}

// AllX is like All, but panics if an error occurs.
func (wraq *WorkspaceRuntimeAccessQuery) AllX(ctx context.Context) []*WorkspaceRuntimeAccess {
	nodes, err := wraq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WorkspaceRuntimeAccess IDs.
func (wraq *WorkspaceRuntimeAccessQuery) IDs(ctx context.Context) (ids []string, err error) {
	if wraq.ctx.Unique == nil && wraq.path != nil {
		wraq.Unique(true)
	}
	ctx = setContextOp(ctx, wraq.ctx, "IDs")
	if err = wraq.Select(workspaceruntimeaccess.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (wraq *WorkspaceRuntimeAccessQuery) IDsX(ctx context.Context) []string {
	ids, err := wraq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (wraq *WorkspaceRuntimeAccessQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, wraq.ctx, "Count")
	if err := wraq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, wraq, querierCount[*WorkspaceRuntimeAccessQuery](), wraq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (wraq *WorkspaceRuntimeAccessQuery) CountX(ctx context.Context) int {
	count, err := wraq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (wraq *WorkspaceRuntimeAccessQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, wraq.ctx, "Exist")
	switch _, err := wraq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (wraq *WorkspaceRuntimeAccessQuery) ExistX(ctx context.Context) bool {
	exist, err := wraq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WorkspaceRuntimeAccessQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (wraq *WorkspaceRuntimeAccessQuery) Clone() *WorkspaceRuntimeAccessQuery {
	if wraq == nil {
		return nil
	}
	return &WorkspaceRuntimeAccessQuery{
		config:     wraq.config,
		ctx:        wraq.ctx.Clone(),
		order:      append([]workspaceruntimeaccess.OrderOption{}, wraq.order...),
		inters:     append([]Interceptor{}, wraq.inters...),
		predicates: append([]predicate.WorkspaceRuntimeAccess{}, wraq.predicates...),
		// clone intermediate query.
		sql:  wraq.sql.Clone(),
		path: wraq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RuntimeID string `json:"runtime_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WorkspaceRuntimeAccess.Query().
//		GroupBy(workspaceruntimeaccess.FieldRuntimeID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (wraq *WorkspaceRuntimeAccessQuery) GroupBy(field string, fields ...string) *WorkspaceRuntimeAccessGroupBy {
	wraq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WorkspaceRuntimeAccessGroupBy{build: wraq}
	grbuild.flds = &wraq.ctx.Fields
	grbuild.label = workspaceruntimeaccess.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RuntimeID string `json:"runtime_id,omitempty"`
//	}
//
//	client.WorkspaceRuntimeAccess.Query().
//		Select(workspaceruntimeaccess.FieldRuntimeID).
//		Scan(ctx, &v)
func (wraq *WorkspaceRuntimeAccessQuery) Select(fields ...string) *WorkspaceRuntimeAccessSelect {
	wraq.ctx.Fields = append(wraq.ctx.Fields, fields...)
	sbuild := &WorkspaceRuntimeAccessSelect{WorkspaceRuntimeAccessQuery: wraq}
	sbuild.label = workspaceruntimeaccess.Label
	sbuild.flds, sbuild.scan = &wraq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WorkspaceRuntimeAccessSelect configured with the given aggregations.
func (wraq *WorkspaceRuntimeAccessQuery) Aggregate(fns ...AggregateFunc) *WorkspaceRuntimeAccessSelect {
	return wraq.Select().Aggregate(fns...)
}

func (wraq *WorkspaceRuntimeAccessQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range wraq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, wraq); err != nil {
				return err
			}
		}
	}
	for _, f := range wraq.ctx.Fields {
		if !workspaceruntimeaccess.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if wraq.path != nil {
		prev, err := wraq.path(ctx)
		if err != nil {
			return err
		}
		wraq.sql = prev
	}
	return nil
}

func (wraq *WorkspaceRuntimeAccessQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WorkspaceRuntimeAccess, error) {
	var (
		nodes = []*WorkspaceRuntimeAccess{}
		_spec = wraq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WorkspaceRuntimeAccess).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WorkspaceRuntimeAccess{config: wraq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, wraq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (wraq *WorkspaceRuntimeAccessQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := wraq.querySpec()
	_spec.Node.Columns = wraq.ctx.Fields
	if len(wraq.ctx.Fields) > 0 {
		_spec.Unique = wraq.ctx.Unique != nil && *wraq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, wraq.driver, _spec)
}

func (wraq *WorkspaceRuntimeAccessQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(workspaceruntimeaccess.Table, workspaceruntimeaccess.Columns, sqlgraph.NewFieldSpec(workspaceruntimeaccess.FieldID, field.TypeString))
	_spec.From = wraq.sql
	if unique := wraq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if wraq.path != nil {
		_spec.Unique = true
	}
	if fields := wraq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, workspaceruntimeaccess.FieldID)
		for i := range fields {
			if fields[i] != workspaceruntimeaccess.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := wraq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := wraq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := wraq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := wraq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (wraq *WorkspaceRuntimeAccessQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(wraq.driver.Dialect())
	t1 := builder.Table(workspaceruntimeaccess.Table)
	columns := wraq.ctx.Fields
	if len(columns) == 0 {
		columns = workspaceruntimeaccess.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wraq.sql != nil {
		selector = wraq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wraq.ctx.Unique != nil && *wraq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wraq.predicates {
		p(selector)
	}
	for _, p := range wraq.order {
		p(selector)
	}
	if offset := wraq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wraq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WorkspaceRuntimeAccessGroupBy is the group-by builder for WorkspaceRuntimeAccess entities.
type WorkspaceRuntimeAccessGroupBy struct {
	selector
	build *WorkspaceRuntimeAccessQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wragb *WorkspaceRuntimeAccessGroupBy) Aggregate(fns ...AggregateFunc) *WorkspaceRuntimeAccessGroupBy {
	wragb.fns = append(wragb.fns, fns...)
	return wragb
}

// Scan applies the selector query and scans the result into the given value.
func (wragb *WorkspaceRuntimeAccessGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wragb.build.ctx, "GroupBy")
	if err := wragb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkspaceRuntimeAccessQuery, *WorkspaceRuntimeAccessGroupBy](ctx, wragb.build, wragb, wragb.build.inters, v)
}

func (wragb *WorkspaceRuntimeAccessGroupBy) sqlScan(ctx context.Context, root *WorkspaceRuntimeAccessQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wragb.fns))
	for _, fn := range wragb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wragb.flds)+len(wragb.fns))
		for _, f := range *wragb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wragb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wragb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WorkspaceRuntimeAccessSelect is the builder for selecting fields of WorkspaceRuntimeAccess entities.
type WorkspaceRuntimeAccessSelect struct {
	*WorkspaceRuntimeAccessQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wras *WorkspaceRuntimeAccessSelect) Aggregate(fns ...AggregateFunc) *WorkspaceRuntimeAccessSelect {
	wras.fns = append(wras.fns, fns...)
	return wras
}

// Scan applies the selector query and scans the result into the given value.
func (wras *WorkspaceRuntimeAccessSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wras.ctx, "Select")
	if err := wras.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkspaceRuntimeAccessQuery, *WorkspaceRuntimeAccessSelect](ctx, wras.WorkspaceRuntimeAccessQuery, wras, wras.inters, v)
}

func (wras *WorkspaceRuntimeAccessSelect) sqlScan(ctx context.Context, root *WorkspaceRuntimeAccessQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wras.fns))
	for _, fn := range wras.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wras.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wras.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
