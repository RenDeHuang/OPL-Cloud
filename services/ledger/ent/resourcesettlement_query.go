// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"opl-cloud/services/ledger/ent/predicate"
	"opl-cloud/services/ledger/ent/resourcesettlement"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ResourceSettlementQuery is the builder for querying ResourceSettlement entities.
type ResourceSettlementQuery struct {
	config
	ctx        *QueryContext
	order      []resourcesettlement.OrderOption
	inters     []Interceptor
	predicates []predicate.ResourceSettlement
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ResourceSettlementQuery builder.
func (rsq *ResourceSettlementQuery) Where(ps ...predicate.ResourceSettlement) *ResourceSettlementQuery {
	rsq.predicates = append(rsq.predicates, ps...)
	return rsq
}

// Limit the number of records to be returned by this query.
func (rsq *ResourceSettlementQuery) Limit(limit int) *ResourceSettlementQuery {
	rsq.ctx.Limit = &limit
	return rsq
}

// Offset to start from.
func (rsq *ResourceSettlementQuery) Offset(offset int) *ResourceSettlementQuery {
	rsq.ctx.Offset = &offset
	return rsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rsq *ResourceSettlementQuery) Unique(unique bool) *ResourceSettlementQuery {
	rsq.ctx.Unique = &unique
	return rsq
}

// Order specifies how the records should be ordered.
func (rsq *ResourceSettlementQuery) Order(o ...resourcesettlement.OrderOption) *ResourceSettlementQuery {
	rsq.order = append(rsq.order, o...)
	return rsq
}

// First returns the first ResourceSettlement entity from the query.
// Returns a *NotFoundError when no ResourceSettlement was found.
func (rsq *ResourceSettlementQuery) First(ctx context.Context) (*ResourceSettlement, error) {
	nodes, err := rsq.Limit(1).All(setContextOp(ctx, rsq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{resourcesettlement.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rsq *ResourceSettlementQuery) FirstX(ctx context.Context) *ResourceSettlement {
	node, err := rsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ResourceSettlement ID from the query.
// Returns a *NotFoundError when no ResourceSettlement ID was found.
func (rsq *ResourceSettlementQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = rsq.Limit(1).IDs(setContextOp(ctx, rsq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{resourcesettlement.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rsq *ResourceSettlementQuery) FirstIDX(ctx context.Context) string {
	id, err := rsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ResourceSettlement entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ResourceSettlement entity is found.
// Returns a *NotFoundError when no ResourceSettlement entities are found.
func (rsq *ResourceSettlementQuery) Only(ctx context.Context) (*ResourceSettlement, error) {
	nodes, err := rsq.Limit(2).All(setContextOp(ctx, rsq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{resourcesettlement.Label}
	default:
		return nil, &NotSingularError{resourcesettlement.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rsq *ResourceSettlementQuery) OnlyX(ctx context.Context) *ResourceSettlement {
	node, err := rsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ResourceSettlement ID in the query.
// Returns a *NotSingularError when more than one ResourceSettlement ID is found.
// Returns a *NotFoundError when no entities are found.
func (rsq *ResourceSettlementQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = rsq.Limit(2).IDs(setContextOp(ctx, rsq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{resourcesettlement.Label}
	default:
		err = &NotSingularError{resourcesettlement.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rsq *ResourceSettlementQuery) OnlyIDX(ctx context.Context) string {
	id, err := rsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ResourceSettlements.
func (rsq *ResourceSettlementQuery) All(ctx context.Context) ([]*ResourceSettlement, error) {
	ctx = setContextOp(ctx, rsq.ctx, "All")
	if err := rsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ResourceSettlement, *ResourceSettlementQuery]()
	return withInterceptors[[]*ResourceSettlement](ctx, rsq, qr, rsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rsq *ResourceSettlementQuery) AllX(ctx context.Context) []*ResourceSettlement {
	nodes, err := rsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ResourceSettlement IDs.
func (rsq *ResourceSettlementQuery) IDs(ctx context.Context) (ids []string, err error) {
	if rsq.ctx.Unique == nil && rsq.path != nil {
		rsq.Unique(true)
	}
	ctx = setContextOp(ctx, rsq.ctx, "IDs")
	if err = rsq.Select(resourcesettlement.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rsq *ResourceSettlementQuery) IDsX(ctx context.Context) []string {
	ids, err := rsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rsq *ResourceSettlementQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rsq.ctx, "Count")
	if err := rsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rsq, querierCount[*ResourceSettlementQuery](), rsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rsq *ResourceSettlementQuery) CountX(ctx context.Context) int {
	count, err := rsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rsq *ResourceSettlementQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rsq.ctx, "Exist")
	switch _, err := rsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rsq *ResourceSettlementQuery) ExistX(ctx context.Context) bool {
	exist, err := rsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ResourceSettlementQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rsq *ResourceSettlementQuery) Clone() *ResourceSettlementQuery {
	if rsq == nil {
		return nil
	}
	return &ResourceSettlementQuery{
		config:     rsq.config,
		ctx:        rsq.ctx.Clone(),
		order:      append([]resourcesettlement.OrderOption{}, rsq.order...),
		inters:     append([]Interceptor{}, rsq.inters...),
		predicates: append([]predicate.ResourceSettlement{}, rsq.predicates...),
		// clone intermediate query.
		sql:  rsq.sql.Clone(),
		path: rsq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		AccountID string `json:"account_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ResourceSettlement.Query().
//		GroupBy(resourcesettlement.FieldAccountID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rsq *ResourceSettlementQuery) GroupBy(field string, fields ...string) *ResourceSettlementGroupBy {
	rsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ResourceSettlementGroupBy{build: rsq}
	grbuild.flds = &rsq.ctx.Fields
	grbuild.label = resourcesettlement.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		AccountID string `json:"account_id,omitempty"`
//	}
//
//	client.ResourceSettlement.Query().
//		Select(resourcesettlement.FieldAccountID).
//		Scan(ctx, &v)
func (rsq *ResourceSettlementQuery) Select(fields ...string) *ResourceSettlementSelect {
	rsq.ctx.Fields = append(rsq.ctx.Fields, fields...)
	sbuild := &ResourceSettlementSelect{ResourceSettlementQuery: rsq}
	sbuild.label = resourcesettlement.Label
	sbuild.flds, sbuild.scan = &rsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ResourceSettlementSelect configured with the given aggregations.
func (rsq *ResourceSettlementQuery) Aggregate(fns ...AggregateFunc) *ResourceSettlementSelect {
	return rsq.Select().Aggregate(fns...)
}

func (rsq *ResourceSettlementQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rsq); err != nil {
				return err
			}
		}
	}
	for _, f := range rsq.ctx.Fields {
		if !resourcesettlement.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rsq.path != nil {
		prev, err := rsq.path(ctx)
		if err != nil {
			return err
		}
		rsq.sql = prev
	}
	return nil
}

func (rsq *ResourceSettlementQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ResourceSettlement, error) {
	var (
		nodes = []*ResourceSettlement{}
		_spec = rsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ResourceSettlement).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ResourceSettlement{config: rsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (rsq *ResourceSettlementQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rsq.querySpec()
	_spec.Node.Columns = rsq.ctx.Fields
	if len(rsq.ctx.Fields) > 0 {
		_spec.Unique = rsq.ctx.Unique != nil && *rsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rsq.driver, _spec)
}

func (rsq *ResourceSettlementQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(resourcesettlement.Table, resourcesettlement.Columns, sqlgraph.NewFieldSpec(resourcesettlement.FieldID, field.TypeString))
	_spec.From = rsq.sql
	if unique := rsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rsq.path != nil {
		_spec.Unique = true
	}
	if fields := rsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, resourcesettlement.FieldID)
		for i := range fields {
			if fields[i] != resourcesettlement.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rsq *ResourceSettlementQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rsq.driver.Dialect())
	t1 := builder.Table(resourcesettlement.Table)
	columns := rsq.ctx.Fields
	if len(columns) == 0 {
		columns = resourcesettlement.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rsq.sql != nil {
		selector = rsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rsq.ctx.Unique != nil && *rsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rsq.predicates {
		p(selector)
	}
	for _, p := range rsq.order {
		p(selector)
	}
	if offset := rsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ResourceSettlementGroupBy is the group-by builder for ResourceSettlement entities.
type ResourceSettlementGroupBy struct {
	selector
	build *ResourceSettlementQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rsgb *ResourceSettlementGroupBy) Aggregate(fns ...AggregateFunc) *ResourceSettlementGroupBy {
	rsgb.fns = append(rsgb.fns, fns...)
	return rsgb
}

// Scan applies the selector query and scans the result into the given value.
func (rsgb *ResourceSettlementGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rsgb.build.ctx, "GroupBy")
	if err := rsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ResourceSettlementQuery, *ResourceSettlementGroupBy](ctx, rsgb.build, rsgb, rsgb.build.inters, v)
}

func (rsgb *ResourceSettlementGroupBy) sqlScan(ctx context.Context, root *ResourceSettlementQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rsgb.fns))
	for _, fn := range rsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rsgb.flds)+len(rsgb.fns))
		for _, f := range *rsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ResourceSettlementSelect is the builder for selecting fields of ResourceSettlement entities.
type ResourceSettlementSelect struct {
	*ResourceSettlementQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rss *ResourceSettlementSelect) Aggregate(fns ...AggregateFunc) *ResourceSettlementSelect {
	rss.fns = append(rss.fns, fns...)
	return rss
}

// Scan applies the selector query and scans the result into the given value.
func (rss *ResourceSettlementSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rss.ctx, "Select")
	if err := rss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ResourceSettlementQuery, *ResourceSettlementSelect](ctx, rss.ResourceSettlementQuery, rss, rss.inters, v)
}

func (rss *ResourceSettlementSelect) sqlScan(ctx context.Context, root *ResourceSettlementQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rss.fns))
	for _, fn := range rss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
