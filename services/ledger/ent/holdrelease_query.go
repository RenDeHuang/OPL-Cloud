// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"opl-cloud/services/ledger/ent/holdrelease"
	"opl-cloud/services/ledger/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// HoldReleaseQuery is the builder for querying HoldRelease entities.
type HoldReleaseQuery struct {
	config
	ctx        *QueryContext
	order      []holdrelease.OrderOption
	inters     []Interceptor
	predicates []predicate.HoldRelease
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HoldReleaseQuery builder.
func (hrq *HoldReleaseQuery) Where(ps ...predicate.HoldRelease) *HoldReleaseQuery {
	hrq.predicates = append(hrq.predicates, ps...)
	return hrq
}

// Limit the number of records to be returned by this query.
func (hrq *HoldReleaseQuery) Limit(limit int) *HoldReleaseQuery {
	hrq.ctx.Limit = &limit
	return hrq
}

// Offset to start from.
func (hrq *HoldReleaseQuery) Offset(offset int) *HoldReleaseQuery {
	hrq.ctx.Offset = &offset
	return hrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (hrq *HoldReleaseQuery) Unique(unique bool) *HoldReleaseQuery {
	hrq.ctx.Unique = &unique
	return hrq
}

// Order specifies how the records should be ordered.
func (hrq *HoldReleaseQuery) Order(o ...holdrelease.OrderOption) *HoldReleaseQuery {
	hrq.order = append(hrq.order, o...)
	return hrq
}

// First returns the first HoldRelease entity from the query.
// Returns a *NotFoundError when no HoldRelease was found.
func (hrq *HoldReleaseQuery) First(ctx context.Context) (*HoldRelease, error) {
	nodes, err := hrq.Limit(1).All(setContextOp(ctx, hrq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{holdrelease.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (hrq *HoldReleaseQuery) FirstX(ctx context.Context) *HoldRelease {
	node, err := hrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first HoldRelease ID from the query.
// Returns a *NotFoundError when no HoldRelease ID was found.
func (hrq *HoldReleaseQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = hrq.Limit(1).IDs(setContextOp(ctx, hrq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{holdrelease.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (hrq *HoldReleaseQuery) FirstIDX(ctx context.Context) string {
	id, err := hrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single HoldRelease entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one HoldRelease entity is found.
// Returns a *NotFoundError when no HoldRelease entities are found.
func (hrq *HoldReleaseQuery) Only(ctx context.Context) (*HoldRelease, error) {
	nodes, err := hrq.Limit(2).All(setContextOp(ctx, hrq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{holdrelease.Label}
	default:
		return nil, &NotSingularError{holdrelease.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (hrq *HoldReleaseQuery) OnlyX(ctx context.Context) *HoldRelease {
	node, err := hrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only HoldRelease ID in the query.
// Returns a *NotSingularError when more than one HoldRelease ID is found.
// Returns a *NotFoundError when no entities are found.
func (hrq *HoldReleaseQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = hrq.Limit(2).IDs(setContextOp(ctx, hrq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{holdrelease.Label}
	default:
		err = &NotSingularError{holdrelease.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (hrq *HoldReleaseQuery) OnlyIDX(ctx context.Context) string {
	id, err := hrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of HoldReleases.
func (hrq *HoldReleaseQuery) All(ctx context.Context) ([]*HoldRelease, error) {
	ctx = setContextOp(ctx, hrq.ctx, "All")
	if err := hrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*HoldRelease, *HoldReleaseQuery]()
	return withInterceptors[[]*HoldRelease](ctx, hrq, qr, hrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (hrq *HoldReleaseQuery) AllX(ctx context.Context) []*HoldRelease {
	nodes, err := hrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of HoldRelease IDs.
func (hrq *HoldReleaseQuery) IDs(ctx context.Context) (ids []string, err error) {
	if hrq.ctx.Unique == nil && hrq.path != nil {
		hrq.Unique(true)
	}
	ctx = setContextOp(ctx, hrq.ctx, "IDs")
	if err = hrq.Select(holdrelease.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (hrq *HoldReleaseQuery) IDsX(ctx context.Context) []string {
	ids, err := hrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (hrq *HoldReleaseQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, hrq.ctx, "Count")
	if err := hrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, hrq, querierCount[*HoldReleaseQuery](), hrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (hrq *HoldReleaseQuery) CountX(ctx context.Context) int {
	count, err := hrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (hrq *HoldReleaseQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, hrq.ctx, "Exist")
	switch _, err := hrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (hrq *HoldReleaseQuery) ExistX(ctx context.Context) bool {
	exist, err := hrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HoldReleaseQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (hrq *HoldReleaseQuery) Clone() *HoldReleaseQuery {
	if hrq == nil {
		return nil
	}
	return &HoldReleaseQuery{
		config:     hrq.config,
		ctx:        hrq.ctx.Clone(),
		order:      append([]holdrelease.OrderOption{}, hrq.order...),
		inters:     append([]Interceptor{}, hrq.inters...),
		predicates: append([]predicate.HoldRelease{}, hrq.predicates...),
		// clone intermediate query.
		sql:  hrq.sql.Clone(),
		path: hrq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		AccountID string `json:"account_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.HoldRelease.Query().
//		GroupBy(holdrelease.FieldAccountID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (hrq *HoldReleaseQuery) GroupBy(field string, fields ...string) *HoldReleaseGroupBy {
	hrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &HoldReleaseGroupBy{build: hrq}
	grbuild.flds = &hrq.ctx.Fields
	grbuild.label = holdrelease.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		AccountID string `json:"account_id,omitempty"`
//	}
//
//	client.HoldRelease.Query().
//		Select(holdrelease.FieldAccountID).
//		Scan(ctx, &v)
func (hrq *HoldReleaseQuery) Select(fields ...string) *HoldReleaseSelect {
	hrq.ctx.Fields = append(hrq.ctx.Fields, fields...)
	sbuild := &HoldReleaseSelect{HoldReleaseQuery: hrq}
	sbuild.label = holdrelease.Label
	sbuild.flds, sbuild.scan = &hrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a HoldReleaseSelect configured with the given aggregations.
func (hrq *HoldReleaseQuery) Aggregate(fns ...AggregateFunc) *HoldReleaseSelect {
	return hrq.Select().Aggregate(fns...)
}

func (hrq *HoldReleaseQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range hrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, hrq); err != nil {
				return err
			}
		}
	}
	for _, f := range hrq.ctx.Fields {
		if !holdrelease.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if hrq.path != nil {
		prev, err := hrq.path(ctx)
		if err != nil {
			return err
		}
		hrq.sql = prev
	}
	return nil
}

func (hrq *HoldReleaseQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*HoldRelease, error) {
	var (
		nodes = []*HoldRelease{}
		_spec = hrq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*HoldRelease).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &HoldRelease{config: hrq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, hrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (hrq *HoldReleaseQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := hrq.querySpec()
	_spec.Node.Columns = hrq.ctx.Fields
	if len(hrq.ctx.Fields) > 0 {
		_spec.Unique = hrq.ctx.Unique != nil && *hrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, hrq.driver, _spec)
}

func (hrq *HoldReleaseQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(holdrelease.Table, holdrelease.Columns, sqlgraph.NewFieldSpec(holdrelease.FieldID, field.TypeString))
	_spec.From = hrq.sql
	if unique := hrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if hrq.path != nil {
		_spec.Unique = true
	}
	if fields := hrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, holdrelease.FieldID)
		for i := range fields {
			if fields[i] != holdrelease.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := hrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := hrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := hrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := hrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (hrq *HoldReleaseQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(hrq.driver.Dialect())
	t1 := builder.Table(holdrelease.Table)
	columns := hrq.ctx.Fields
	if len(columns) == 0 {
		columns = holdrelease.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if hrq.sql != nil {
		selector = hrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if hrq.ctx.Unique != nil && *hrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range hrq.predicates {
		p(selector)
	}
	for _, p := range hrq.order {
		p(selector)
	}
	if offset := hrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := hrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// HoldReleaseGroupBy is the group-by builder for HoldRelease entities.
type HoldReleaseGroupBy struct {
	selector
	build *HoldReleaseQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (hrgb *HoldReleaseGroupBy) Aggregate(fns ...AggregateFunc) *HoldReleaseGroupBy {
	hrgb.fns = append(hrgb.fns, fns...)
	return hrgb
}

// Scan applies the selector query and scans the result into the given value.
func (hrgb *HoldReleaseGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hrgb.build.ctx, "GroupBy")
	if err := hrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HoldReleaseQuery, *HoldReleaseGroupBy](ctx, hrgb.build, hrgb, hrgb.build.inters, v)
}

func (hrgb *HoldReleaseGroupBy) sqlScan(ctx context.Context, root *HoldReleaseQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(hrgb.fns))
	for _, fn := range hrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*hrgb.flds)+len(hrgb.fns))
		for _, f := range *hrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*hrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// HoldReleaseSelect is the builder for selecting fields of HoldRelease entities.
type HoldReleaseSelect struct {
	*HoldReleaseQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (hrs *HoldReleaseSelect) Aggregate(fns ...AggregateFunc) *HoldReleaseSelect {
	hrs.fns = append(hrs.fns, fns...)
	return hrs
}

// Scan applies the selector query and scans the result into the given value.
func (hrs *HoldReleaseSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hrs.ctx, "Select")
	if err := hrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HoldReleaseQuery, *HoldReleaseSelect](ctx, hrs.HoldReleaseQuery, hrs, hrs.inters, v)
}

func (hrs *HoldReleaseSelect) sqlScan(ctx context.Context, root *HoldReleaseQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(hrs.fns))
	for _, fn := range hrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*hrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
