// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"opl-cloud/services/ledger/ent/holdactivation"
	"opl-cloud/services/ledger/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// HoldActivationQuery is the builder for querying HoldActivation entities.
type HoldActivationQuery struct {
	config
	ctx        *QueryContext
	order      []holdactivation.OrderOption
	inters     []Interceptor
	predicates []predicate.HoldActivation
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HoldActivationQuery builder.
func (haq *HoldActivationQuery) Where(ps ...predicate.HoldActivation) *HoldActivationQuery {
	haq.predicates = append(haq.predicates, ps...)
	return haq
}

// Limit the number of records to be returned by this query.
func (haq *HoldActivationQuery) Limit(limit int) *HoldActivationQuery {
	haq.ctx.Limit = &limit
	return haq
}

// Offset to start from.
func (haq *HoldActivationQuery) Offset(offset int) *HoldActivationQuery {
	haq.ctx.Offset = &offset
	return haq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (haq *HoldActivationQuery) Unique(unique bool) *HoldActivationQuery {
	haq.ctx.Unique = &unique
	return haq
}

// Order specifies how the records should be ordered.
func (haq *HoldActivationQuery) Order(o ...holdactivation.OrderOption) *HoldActivationQuery {
	haq.order = append(haq.order, o...)
	return haq
}

// First returns the first HoldActivation entity from the query.
// Returns a *NotFoundError when no HoldActivation was found.
func (haq *HoldActivationQuery) First(ctx context.Context) (*HoldActivation, error) {
	nodes, err := haq.Limit(1).All(setContextOp(ctx, haq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{holdactivation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (haq *HoldActivationQuery) FirstX(ctx context.Context) *HoldActivation {
	node, err := haq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first HoldActivation ID from the query.
// Returns a *NotFoundError when no HoldActivation ID was found.
func (haq *HoldActivationQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = haq.Limit(1).IDs(setContextOp(ctx, haq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{holdactivation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (haq *HoldActivationQuery) FirstIDX(ctx context.Context) string {
	id, err := haq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single HoldActivation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one HoldActivation entity is found.
// Returns a *NotFoundError when no HoldActivation entities are found.
func (haq *HoldActivationQuery) Only(ctx context.Context) (*HoldActivation, error) {
	nodes, err := haq.Limit(2).All(setContextOp(ctx, haq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{holdactivation.Label}
	default:
		return nil, &NotSingularError{holdactivation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (haq *HoldActivationQuery) OnlyX(ctx context.Context) *HoldActivation {
	node, err := haq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only HoldActivation ID in the query.
// Returns a *NotSingularError when more than one HoldActivation ID is found.
// Returns a *NotFoundError when no entities are found.
func (haq *HoldActivationQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = haq.Limit(2).IDs(setContextOp(ctx, haq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{holdactivation.Label}
	default:
		err = &NotSingularError{holdactivation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (haq *HoldActivationQuery) OnlyIDX(ctx context.Context) string {
	id, err := haq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of HoldActivations.
func (haq *HoldActivationQuery) All(ctx context.Context) ([]*HoldActivation, error) {
	ctx = setContextOp(ctx, haq.ctx, "All")
	if err := haq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*HoldActivation, *HoldActivationQuery]()
	return withInterceptors[[]*HoldActivation](ctx, haq, qr, haq.inters)
}

// AllX is like All, but panics if an error occurs.
func (haq *HoldActivationQuery) AllX(ctx context.Context) []*HoldActivation {
	nodes, err := haq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of HoldActivation IDs.
func (haq *HoldActivationQuery) IDs(ctx context.Context) (ids []string, err error) {
	if haq.ctx.Unique == nil && haq.path != nil {
		haq.Unique(true)
	}
	ctx = setContextOp(ctx, haq.ctx, "IDs")
	if err = haq.Select(holdactivation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (haq *HoldActivationQuery) IDsX(ctx context.Context) []string {
	ids, err := haq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (haq *HoldActivationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, haq.ctx, "Count")
	if err := haq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, haq, querierCount[*HoldActivationQuery](), haq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (haq *HoldActivationQuery) CountX(ctx context.Context) int {
	count, err := haq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (haq *HoldActivationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, haq.ctx, "Exist")
	switch _, err := haq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (haq *HoldActivationQuery) ExistX(ctx context.Context) bool {
	exist, err := haq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HoldActivationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (haq *HoldActivationQuery) Clone() *HoldActivationQuery {
	if haq == nil {
		return nil
	}
	return &HoldActivationQuery{
		config:     haq.config,
		ctx:        haq.ctx.Clone(),
		order:      append([]holdactivation.OrderOption{}, haq.order...),
		inters:     append([]Interceptor{}, haq.inters...),
		predicates: append([]predicate.HoldActivation{}, haq.predicates...),
		// clone intermediate query.
		sql:  haq.sql.Clone(),
		path: haq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		AccountID string `json:"account_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.HoldActivation.Query().
//		GroupBy(holdactivation.FieldAccountID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (haq *HoldActivationQuery) GroupBy(field string, fields ...string) *HoldActivationGroupBy {
	haq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &HoldActivationGroupBy{build: haq}
	grbuild.flds = &haq.ctx.Fields
	grbuild.label = holdactivation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		AccountID string `json:"account_id,omitempty"`
//	}
//
//	client.HoldActivation.Query().
//		Select(holdactivation.FieldAccountID).
//		Scan(ctx, &v)
func (haq *HoldActivationQuery) Select(fields ...string) *HoldActivationSelect {
	haq.ctx.Fields = append(haq.ctx.Fields, fields...)
	sbuild := &HoldActivationSelect{HoldActivationQuery: haq}
	sbuild.label = holdactivation.Label
	sbuild.flds, sbuild.scan = &haq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a HoldActivationSelect configured with the given aggregations.
func (haq *HoldActivationQuery) Aggregate(fns ...AggregateFunc) *HoldActivationSelect {
	return haq.Select().Aggregate(fns...)
}

func (haq *HoldActivationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range haq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, haq); err != nil {
				return err
			}
		}
	}
	for _, f := range haq.ctx.Fields {
		if !holdactivation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if haq.path != nil {
		prev, err := haq.path(ctx)
		if err != nil {
			return err
		}
		haq.sql = prev
	}
	return nil
}

func (haq *HoldActivationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*HoldActivation, error) {
	var (
		nodes = []*HoldActivation{}
		_spec = haq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*HoldActivation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &HoldActivation{config: haq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, haq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (haq *HoldActivationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := haq.querySpec()
	_spec.Node.Columns = haq.ctx.Fields
	if len(haq.ctx.Fields) > 0 {
		_spec.Unique = haq.ctx.Unique != nil && *haq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, haq.driver, _spec)
}

func (haq *HoldActivationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(holdactivation.Table, holdactivation.Columns, sqlgraph.NewFieldSpec(holdactivation.FieldID, field.TypeString))
	_spec.From = haq.sql
	if unique := haq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if haq.path != nil {
		_spec.Unique = true
	}
	if fields := haq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, holdactivation.FieldID)
		for i := range fields {
			if fields[i] != holdactivation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := haq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := haq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := haq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := haq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (haq *HoldActivationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(haq.driver.Dialect())
	t1 := builder.Table(holdactivation.Table)
	columns := haq.ctx.Fields
	if len(columns) == 0 {
		columns = holdactivation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if haq.sql != nil {
		selector = haq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if haq.ctx.Unique != nil && *haq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range haq.predicates {
		p(selector)
	}
	for _, p := range haq.order {
		p(selector)
	}
	if offset := haq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := haq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// HoldActivationGroupBy is the group-by builder for HoldActivation entities.
type HoldActivationGroupBy struct {
	selector
	build *HoldActivationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (hagb *HoldActivationGroupBy) Aggregate(fns ...AggregateFunc) *HoldActivationGroupBy {
	hagb.fns = append(hagb.fns, fns...)
	return hagb
}

// Scan applies the selector query and scans the result into the given value.
func (hagb *HoldActivationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hagb.build.ctx, "GroupBy")
	if err := hagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HoldActivationQuery, *HoldActivationGroupBy](ctx, hagb.build, hagb, hagb.build.inters, v)
}

func (hagb *HoldActivationGroupBy) sqlScan(ctx context.Context, root *HoldActivationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(hagb.fns))
	for _, fn := range hagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*hagb.flds)+len(hagb.fns))
		for _, f := range *hagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*hagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// HoldActivationSelect is the builder for selecting fields of HoldActivation entities.
type HoldActivationSelect struct {
	*HoldActivationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (has *HoldActivationSelect) Aggregate(fns ...AggregateFunc) *HoldActivationSelect {
	has.fns = append(has.fns, fns...)
	return has
}

// Scan applies the selector query and scans the result into the given value.
func (has *HoldActivationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, has.ctx, "Select")
	if err := has.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HoldActivationQuery, *HoldActivationSelect](ctx, has.HoldActivationQuery, has, has.inters, v)
}

func (has *HoldActivationSelect) sqlScan(ctx context.Context, root *HoldActivationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(has.fns))
	for _, fn := range has.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*has.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := has.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
