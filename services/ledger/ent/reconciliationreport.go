// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"opl-cloud/services/ledger/ent/reconciliationreport"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ReconciliationReport is the model entity for the ReconciliationReport schema.
type ReconciliationReport struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// ReportJSON holds the value of the "report_json" field.
	ReportJSON string `json:"report_json,omitempty"`
	// BlockNewWorkspaces holds the value of the "block_new_workspaces" field.
	BlockNewWorkspaces bool `json:"block_new_workspaces,omitempty"`
	// Reason holds the value of the "reason" field.
	Reason string `json:"reason,omitempty"`
	// IdempotencyKey holds the value of the "idempotency_key" field.
	IdempotencyKey string `json:"idempotency_key,omitempty"`
	// RequestHash holds the value of the "request_hash" field.
	RequestHash string `json:"request_hash,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ReconciliationReport) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case reconciliationreport.FieldBlockNewWorkspaces:
			values[i] = new(sql.NullBool)
		case reconciliationreport.FieldID, reconciliationreport.FieldStatus, reconciliationreport.FieldReportJSON, reconciliationreport.FieldReason, reconciliationreport.FieldIdempotencyKey, reconciliationreport.FieldRequestHash:
			values[i] = new(sql.NullString)
		case reconciliationreport.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ReconciliationReport fields.
func (rr *ReconciliationReport) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case reconciliationreport.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				rr.ID = value.String
			}
		case reconciliationreport.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				rr.Status = value.String
			}
		case reconciliationreport.FieldReportJSON:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field report_json", values[i])
			} else if value.Valid {
				rr.ReportJSON = value.String
			}
		case reconciliationreport.FieldBlockNewWorkspaces:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field block_new_workspaces", values[i])
			} else if value.Valid {
				rr.BlockNewWorkspaces = value.Bool
			}
		case reconciliationreport.FieldReason:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field reason", values[i])
			} else if value.Valid {
				rr.Reason = value.String
			}
		case reconciliationreport.FieldIdempotencyKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field idempotency_key", values[i])
			} else if value.Valid {
				rr.IdempotencyKey = value.String
			}
		case reconciliationreport.FieldRequestHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field request_hash", values[i])
			} else if value.Valid {
				rr.RequestHash = value.String
			}
		case reconciliationreport.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				rr.CreatedAt = value.Time
			}
		default:
			rr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ReconciliationReport.
// This includes values selected through modifiers, order, etc.
func (rr *ReconciliationReport) Value(name string) (ent.Value, error) {
	return rr.selectValues.Get(name)
}

// Update returns a builder for updating this ReconciliationReport.
// Note that you need to call ReconciliationReport.Unwrap() before calling this method if this ReconciliationReport
// was returned from a transaction, and the transaction was committed or rolled back.
func (rr *ReconciliationReport) Update() *ReconciliationReportUpdateOne {
	return NewReconciliationReportClient(rr.config).UpdateOne(rr)
}

// Unwrap unwraps the ReconciliationReport entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (rr *ReconciliationReport) Unwrap() *ReconciliationReport {
	_tx, ok := rr.config.driver.(*txDriver)
	if !ok {
		panic("ent: ReconciliationReport is not a transactional entity")
	}
	rr.config.driver = _tx.drv
	return rr
}

// String implements the fmt.Stringer.
func (rr *ReconciliationReport) String() string {
	var builder strings.Builder
	builder.WriteString("ReconciliationReport(")
	builder.WriteString(fmt.Sprintf("id=%v, ", rr.ID))
	builder.WriteString("status=")
	builder.WriteString(rr.Status)
	builder.WriteString(", ")
	builder.WriteString("report_json=")
	builder.WriteString(rr.ReportJSON)
	builder.WriteString(", ")
	builder.WriteString("block_new_workspaces=")
	builder.WriteString(fmt.Sprintf("%v", rr.BlockNewWorkspaces))
	builder.WriteString(", ")
	builder.WriteString("reason=")
	builder.WriteString(rr.Reason)
	builder.WriteString(", ")
	builder.WriteString("idempotency_key=")
	builder.WriteString(rr.IdempotencyKey)
	builder.WriteString(", ")
	builder.WriteString("request_hash=")
	builder.WriteString(rr.RequestHash)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(rr.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ReconciliationReports is a parsable slice of ReconciliationReport.
type ReconciliationReports []*ReconciliationReport
