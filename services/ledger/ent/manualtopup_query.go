// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"opl-cloud/services/ledger/ent/manualtopup"
	"opl-cloud/services/ledger/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ManualTopupQuery is the builder for querying ManualTopup entities.
type ManualTopupQuery struct {
	config
	ctx        *QueryContext
	order      []manualtopup.OrderOption
	inters     []Interceptor
	predicates []predicate.ManualTopup
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ManualTopupQuery builder.
func (mtq *ManualTopupQuery) Where(ps ...predicate.ManualTopup) *ManualTopupQuery {
	mtq.predicates = append(mtq.predicates, ps...)
	return mtq
}

// Limit the number of records to be returned by this query.
func (mtq *ManualTopupQuery) Limit(limit int) *ManualTopupQuery {
	mtq.ctx.Limit = &limit
	return mtq
}

// Offset to start from.
func (mtq *ManualTopupQuery) Offset(offset int) *ManualTopupQuery {
	mtq.ctx.Offset = &offset
	return mtq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mtq *ManualTopupQuery) Unique(unique bool) *ManualTopupQuery {
	mtq.ctx.Unique = &unique
	return mtq
}

// Order specifies how the records should be ordered.
func (mtq *ManualTopupQuery) Order(o ...manualtopup.OrderOption) *ManualTopupQuery {
	mtq.order = append(mtq.order, o...)
	return mtq
}

// First returns the first ManualTopup entity from the query.
// Returns a *NotFoundError when no ManualTopup was found.
func (mtq *ManualTopupQuery) First(ctx context.Context) (*ManualTopup, error) {
	nodes, err := mtq.Limit(1).All(setContextOp(ctx, mtq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{manualtopup.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mtq *ManualTopupQuery) FirstX(ctx context.Context) *ManualTopup {
	node, err := mtq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ManualTopup ID from the query.
// Returns a *NotFoundError when no ManualTopup ID was found.
func (mtq *ManualTopupQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = mtq.Limit(1).IDs(setContextOp(ctx, mtq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{manualtopup.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mtq *ManualTopupQuery) FirstIDX(ctx context.Context) string {
	id, err := mtq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ManualTopup entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ManualTopup entity is found.
// Returns a *NotFoundError when no ManualTopup entities are found.
func (mtq *ManualTopupQuery) Only(ctx context.Context) (*ManualTopup, error) {
	nodes, err := mtq.Limit(2).All(setContextOp(ctx, mtq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{manualtopup.Label}
	default:
		return nil, &NotSingularError{manualtopup.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mtq *ManualTopupQuery) OnlyX(ctx context.Context) *ManualTopup {
	node, err := mtq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ManualTopup ID in the query.
// Returns a *NotSingularError when more than one ManualTopup ID is found.
// Returns a *NotFoundError when no entities are found.
func (mtq *ManualTopupQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = mtq.Limit(2).IDs(setContextOp(ctx, mtq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{manualtopup.Label}
	default:
		err = &NotSingularError{manualtopup.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mtq *ManualTopupQuery) OnlyIDX(ctx context.Context) string {
	id, err := mtq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ManualTopups.
func (mtq *ManualTopupQuery) All(ctx context.Context) ([]*ManualTopup, error) {
	ctx = setContextOp(ctx, mtq.ctx, "All")
	if err := mtq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ManualTopup, *ManualTopupQuery]()
	return withInterceptors[[]*ManualTopup](ctx, mtq, qr, mtq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mtq *ManualTopupQuery) AllX(ctx context.Context) []*ManualTopup {
	nodes, err := mtq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ManualTopup IDs.
func (mtq *ManualTopupQuery) IDs(ctx context.Context) (ids []string, err error) {
	if mtq.ctx.Unique == nil && mtq.path != nil {
		mtq.Unique(true)
	}
	ctx = setContextOp(ctx, mtq.ctx, "IDs")
	if err = mtq.Select(manualtopup.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mtq *ManualTopupQuery) IDsX(ctx context.Context) []string {
	ids, err := mtq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mtq *ManualTopupQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mtq.ctx, "Count")
	if err := mtq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mtq, querierCount[*ManualTopupQuery](), mtq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mtq *ManualTopupQuery) CountX(ctx context.Context) int {
	count, err := mtq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mtq *ManualTopupQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mtq.ctx, "Exist")
	switch _, err := mtq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mtq *ManualTopupQuery) ExistX(ctx context.Context) bool {
	exist, err := mtq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ManualTopupQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mtq *ManualTopupQuery) Clone() *ManualTopupQuery {
	if mtq == nil {
		return nil
	}
	return &ManualTopupQuery{
		config:     mtq.config,
		ctx:        mtq.ctx.Clone(),
		order:      append([]manualtopup.OrderOption{}, mtq.order...),
		inters:     append([]Interceptor{}, mtq.inters...),
		predicates: append([]predicate.ManualTopup{}, mtq.predicates...),
		// clone intermediate query.
		sql:  mtq.sql.Clone(),
		path: mtq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		AccountID string `json:"account_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ManualTopup.Query().
//		GroupBy(manualtopup.FieldAccountID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mtq *ManualTopupQuery) GroupBy(field string, fields ...string) *ManualTopupGroupBy {
	mtq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ManualTopupGroupBy{build: mtq}
	grbuild.flds = &mtq.ctx.Fields
	grbuild.label = manualtopup.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		AccountID string `json:"account_id,omitempty"`
//	}
//
//	client.ManualTopup.Query().
//		Select(manualtopup.FieldAccountID).
//		Scan(ctx, &v)
func (mtq *ManualTopupQuery) Select(fields ...string) *ManualTopupSelect {
	mtq.ctx.Fields = append(mtq.ctx.Fields, fields...)
	sbuild := &ManualTopupSelect{ManualTopupQuery: mtq}
	sbuild.label = manualtopup.Label
	sbuild.flds, sbuild.scan = &mtq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ManualTopupSelect configured with the given aggregations.
func (mtq *ManualTopupQuery) Aggregate(fns ...AggregateFunc) *ManualTopupSelect {
	return mtq.Select().Aggregate(fns...)
}

func (mtq *ManualTopupQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mtq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mtq); err != nil {
				return err
			}
		}
	}
	for _, f := range mtq.ctx.Fields {
		if !manualtopup.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mtq.path != nil {
		prev, err := mtq.path(ctx)
		if err != nil {
			return err
		}
		mtq.sql = prev
	}
	return nil
}

func (mtq *ManualTopupQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ManualTopup, error) {
	var (
		nodes = []*ManualTopup{}
		_spec = mtq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ManualTopup).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ManualTopup{config: mtq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mtq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (mtq *ManualTopupQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mtq.querySpec()
	_spec.Node.Columns = mtq.ctx.Fields
	if len(mtq.ctx.Fields) > 0 {
		_spec.Unique = mtq.ctx.Unique != nil && *mtq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mtq.driver, _spec)
}

func (mtq *ManualTopupQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(manualtopup.Table, manualtopup.Columns, sqlgraph.NewFieldSpec(manualtopup.FieldID, field.TypeString))
	_spec.From = mtq.sql
	if unique := mtq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mtq.path != nil {
		_spec.Unique = true
	}
	if fields := mtq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, manualtopup.FieldID)
		for i := range fields {
			if fields[i] != manualtopup.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mtq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mtq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mtq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mtq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mtq *ManualTopupQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mtq.driver.Dialect())
	t1 := builder.Table(manualtopup.Table)
	columns := mtq.ctx.Fields
	if len(columns) == 0 {
		columns = manualtopup.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mtq.sql != nil {
		selector = mtq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mtq.ctx.Unique != nil && *mtq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range mtq.predicates {
		p(selector)
	}
	for _, p := range mtq.order {
		p(selector)
	}
	if offset := mtq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mtq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ManualTopupGroupBy is the group-by builder for ManualTopup entities.
type ManualTopupGroupBy struct {
	selector
	build *ManualTopupQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mtgb *ManualTopupGroupBy) Aggregate(fns ...AggregateFunc) *ManualTopupGroupBy {
	mtgb.fns = append(mtgb.fns, fns...)
	return mtgb
}

// Scan applies the selector query and scans the result into the given value.
func (mtgb *ManualTopupGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mtgb.build.ctx, "GroupBy")
	if err := mtgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ManualTopupQuery, *ManualTopupGroupBy](ctx, mtgb.build, mtgb, mtgb.build.inters, v)
}

func (mtgb *ManualTopupGroupBy) sqlScan(ctx context.Context, root *ManualTopupQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mtgb.fns))
	for _, fn := range mtgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mtgb.flds)+len(mtgb.fns))
		for _, f := range *mtgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mtgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mtgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ManualTopupSelect is the builder for selecting fields of ManualTopup entities.
type ManualTopupSelect struct {
	*ManualTopupQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mts *ManualTopupSelect) Aggregate(fns ...AggregateFunc) *ManualTopupSelect {
	mts.fns = append(mts.fns, fns...)
	return mts
}

// Scan applies the selector query and scans the result into the given value.
func (mts *ManualTopupSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mts.ctx, "Select")
	if err := mts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ManualTopupQuery, *ManualTopupSelect](ctx, mts.ManualTopupQuery, mts, mts.inters, v)
}

func (mts *ManualTopupSelect) sqlScan(ctx context.Context, root *ManualTopupQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mts.fns))
	for _, fn := range mts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
